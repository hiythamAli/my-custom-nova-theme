NOVA Product Card Styles — per-element component patch

Important:
Salla predefined Theme Features cannot have per-instance custom fields in twilight.json. This patch keeps the original predefined features intact and adds Nova custom component versions with a per-element "شكل بطاقات المنتجات" dropdown. The existing custom "منتجات متحركة مع خلفية" component keeps its independent selector.

Custom Nova product components added:
- بطاقات مميزة — Nova 1
- بطاقات مميزة — Nova 2
- بطاقات مميزة — Nova 3
- بطاقات ثابتة — Nova
- بطاقات متحركة — Nova
- أحدث المنتجات — Nova

Modern remains available and existing theme/card styling is preserved.
